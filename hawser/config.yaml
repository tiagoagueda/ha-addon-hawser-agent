name: Hawser Agent
slug: hawser
version: "1.1.8"
description: Hawser agent for Dockhand (Edge mode)

arch:
  - amd64
  - aarch64

startup: services
boot: auto

docker_api: true
full_access: true

host_network: false


options:
  dockhand_server_url: ""
  token: ""

schema:
  dockhand_server_url: str
  token: password
